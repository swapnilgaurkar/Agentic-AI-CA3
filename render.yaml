services:
  - type: web
    name: discount-agent
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python discount_agent.py
    envVars:
      - key: RENDER
        value: "true"
      - key: PYTHON_VERSION
        value: "3.11.0"
